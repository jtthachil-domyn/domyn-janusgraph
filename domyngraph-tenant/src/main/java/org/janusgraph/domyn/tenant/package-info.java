/**
 * DomynGraph Tenant Engine — DB-level multi-tenant isolation for JanusGraph.
 *
 * Provides tenant lifecycle management (create, open, drop), schema versioning
 * with migrations, tenant-aware traversal sources, and per-tenant Elasticsearch
 * index configuration.
 */
package org.janusgraph.domyn.tenant;
