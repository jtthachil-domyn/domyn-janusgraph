[package]
name = "nexus-algebra"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "GraphBLAS-inspired sparse matrix algebra: SpMV, SpMM, semiring traits, masked operations (FalkorDB-inspired)"

[dependencies]
nexus-core = { workspace = true }
rayon = { workspace = true }
smallvec = { workspace = true }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "spmv_bench"
harness = false
